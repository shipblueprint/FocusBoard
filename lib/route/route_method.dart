import 'package:focusboard/route/routes_name.dart';

/// Single global instance of the [RoutesName] registry.
///
/// Used as `route.kanban`, `route.eisenhower`, etc. throughout the app.
final RoutesName route = RoutesName();
